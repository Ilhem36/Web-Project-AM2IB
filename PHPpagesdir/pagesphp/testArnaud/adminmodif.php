<?php require_once 'db_utils.php';
connect_db();
    if(isset($_POST['submit']))
    {
    #pour toutes les lignes du tableau
        #si checkbox cochée
            #requete de supression de la ligne dans la base de donnée avec clé
        #si rôle différent de role bd
            #rôle prend nouvelle valeur
        #astuce : creer array php, créer table html qui s'affiche avec css
disconnect_db();
?>

